components:
  schemas:
    VehicleTypeDto:
      title: VehicleTypeDto
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        hourlyRate:
          type: number
          format: double
        capacity:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - hourlyRate
        - capacity
        - createdAt
        - updatedAt
